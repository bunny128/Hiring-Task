# Hiring-Task
 A Python-based tender PDF parser that extracts Form and Annexure metadata. It detects section headings, classifies them, infers page ranges, and returns structured JSON with type, name, title, and pages. It uses PyMuPDF for text extraction and PaddleOCR as a fallback for scanned pages.
